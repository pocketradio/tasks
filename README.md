# Tasks

A command-line task manager written in Go.

## Commands

- `add` – add a task
- `do` – mark a task as done
- `list` – list all tasks

## Install & Run

```bash
go install .
task --help
```

## Running a command

```bash
task <command_name>
```
